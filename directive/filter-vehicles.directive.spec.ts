import { FilterVehiclesDirective } from './filter-vehicles.directive';

describe('FilterVehiclesDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterVehiclesDirective();
    expect(directive).toBeTruthy();
  });
});
